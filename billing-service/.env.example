# Copiar como .env y ajustar si es necesario. Nunca commitees el .env real.
# Puerto 5438 en el host: del 5433 al 5437 ya los usan las bases de Catalog,
# Playback, User, Media y Recommendation.
DATABASE_URL="postgresql://billing_service:billing_service@localhost:5438/billing_service_db?schema=public"

# IMPORTANTE: debe apuntar al MISMO RabbitMQ que usa User-Service, para que
# el evento payment.failed le llegue. Con la plataforma completa es el de
# localhost:5672 (con el docker-compose aislado de esta carpeta, el 5674).
RABBITMQ_URL="amqp://guest:guest@localhost:5672"

# Solo se usaría con una integración real de Stripe (no se usa en la
# simulación actual). Ver src/stripe/stripe.service.ts.
STRIPE_SECRET_KEY="sk_test_no_usada_en_esta_simulacion"

PORT=3006
NODE_ENV=development
CORS_ORIGINS="*"

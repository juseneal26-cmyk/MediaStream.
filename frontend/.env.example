# Copiar como .env y ajustar. Se inyecta en BUILD TIME (Vite), no en runtime:
# hay que reconstruir el frontend si esto cambia (sección 8.1 del documento).

# URL base del API Gateway — el frontend NUNCA llama a un microservicio
# directamente, solo al Gateway.
VITE_API_GATEWAY_URL=http://localhost:3000
